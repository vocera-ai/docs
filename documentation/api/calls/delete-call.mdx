---
title: 'Deleting Calls'
description: 'Learn how to delete existing call logs'
---

# Delete Call API

Delete an existing call log.

## API Endpoint

| Method | Endpoint |
|--------|----------|
| `DELETE` | `https://api.cekura.ai/observability/v1/call-logs-external/:call_log_id/` |

## Authentication

Include your API key in the request headers:

| Header | Description |
|--------|-------------|
| `X-CEKURA-API-KEY` | Your API key obtained from the dashboard |

## Path Parameters

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `call_log_id` | integer | Yes | The ID of the call log to delete |

## Response

A successful deletion returns a 204 No Content response.

## Code Examples

<CodeGroup>

```bash Curl
curl -X DELETE https://api.cekura.ai/observability/v1/call-logs-external/:call_log_id/ \
  -H "X-CEKURA-API-KEY: <your-api-key-here>"
```

```python Python
import requests

def delete_call(api_key, call_log_id):
    url = f'https://api.cekura.ai/observability/v1/call-logs-external/{call_log_id}/'
    
    headers = {
        'X-CEKURA-API-KEY': api_key
    }

    response = requests.delete(url, headers=headers)
    return response.status_code == 204
```

```javascript JavaScript
async function deleteCall(apiKey, callLogId) {
  const url = `https://api.cekura.ai/observability/v1/call-logs-external/${callLogId}/`;
  
  const response = await fetch(url, {
    method: 'DELETE',
    headers: {
      'X-CEKURA-API-KEY': apiKey
    }
  });

  return response.status === 204;
}
```

</CodeGroup>
